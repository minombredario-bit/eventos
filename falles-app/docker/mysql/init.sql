-- Database initialization for Falles App
-- This runs automatically when the MySQL container starts

-- Character set configuration
ALTER DATABASE falles_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- You can add initial seed data here if needed
-- Example:
-- INSERT INTO entidad (id, nombre, tipo, email, activo, fechaCreacion) 
-- VALUES (1, 'Falla Example', 'FALLA', 'admin@example.com', true, NOW());
